# Правила коммитов

Префикс `feat` используется только при изменениях, связанных с Dockerfile, Go-кода и образами (image). Все прочие изменения коммитить с другими префиксами (`fix`, `docs`, `chore`, `refactor`, `ci` и т.д.).
